<?xml version="1.0" encoding="utf-8"?>
<manifest package="io.realm.examples.adapters"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="yobe.sandboxralm.MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name="yobe.sandboxralm.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="yobe.sandboxralm.ui.listview.ListViewExampleActivity"
            android:label="ListView Example"/>
        <activity
            android:name="yobe.sandboxralm.ui.recycleview.RecyclerViewExampleActivity"
            android:label="RecyclerView Example"/>
    </application>

</manifest>